# Copy to .env and fill in to use real providers. Defaults are mocks --
# the service runs with none of these set.

TRANSCRIPTION_PROVIDER=mock   # mock | whisper
EXTRACTION_PROVIDER=mock      # mock | tesseract

OPENAI_API_KEY=

MAX_UPLOAD_MB=25
